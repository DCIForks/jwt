# Simplistic Demo of JWT

Result of a live-coding demo.

## Routes
`/` returns a welcome message  
`/token` returns a JSON Web Token which expires after 2 minutes  
`/api?token=<token>` returns fictitious bank details  
